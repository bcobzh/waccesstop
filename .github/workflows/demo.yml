name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  meta:
    name: Meta
    permissions:
      contents: 'read'
      id-token: 'write'
      pull-requests: 'write'
    outputs:
      kc_secret: MYSECRET
      kc_app_id: MYAPPID

    runs-on: ubuntu-latest
    steps:
      - name: swo env
        run: |
          env
  deploy:
    name: deploy
    needs: [ "meta" ]
    runs-on: ubuntu-latest
    steps:
      - name: show env
        run: |
          env
      - name: install
        run: |
          sudo apt install maven s3cmd
      - name: show remote
        run: |
          echo '${{ toJSON(needs.meta.outputs) }}'  | jq

